// `$()` expands to an empty build setting and prevents `//` from starting an
// xcconfig comment inside the URL.
SUPABASE_URL = https:/$()/amgezynqenbgopnnpxso.supabase.co
SUPABASE_ANON_KEY =

#include? "Secrets.xcconfig"
